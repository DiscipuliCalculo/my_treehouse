extends layout

block content

  if user

    if pass_result = true
      h5 Join the Club! 
      
      h6 Correct! You're in the club

    if pass_result = false 
      h5 Join the Club! 

      h6 Sorry that is not the right word
      form(method='POST' action='')
        p What is the secret word?
        input(type='text')
        input(type='submit')

    else
      h5 Join the Club! 

      form(method='POST' action='')
        p What is the secret word?
        input(type='text')
        input(type='submit')

  else
    h4 You are not logged in
  